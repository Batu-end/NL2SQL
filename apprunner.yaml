# app runner yaml dependencies generated by Gemini 2.5 Pro
version: 1.0
runtime: python3.11
build:
  commands:
    pre-build:
      - pip install -r requirements.txt
run:
  command: uvicorn main:app --host 0.0.0.0 --port 8080
  network:
    port: 8080